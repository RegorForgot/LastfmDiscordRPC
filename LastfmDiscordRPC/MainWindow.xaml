<Window x:Class="LastfmDiscordRPC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:LastfmDiscordRPC.Controls"
        mc:Ignorable="d"
        Title="Last.fm Discord Rich Presence"
        Height="600" Width="900"
        ResizeMode="CanMinimize" 
        d:DataContext="{d:DesignInstance}">
    
    <Window.Background>
        <LinearGradientBrush EndPoint="1.0, 0.5" StartPoint="0.0, 0.0">
            <GradientStop Color="#9094a5"/>
            <GradientStop Color="#56595f" Offset="2"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <c:Header/>
        <c:Body Grid.Row="1"/>
        <c:Footer Grid.Row="2"
                  VerticalAlignment="Bottom"/>
    </Grid>
</Window>